import { SeriesCard } from "../SeriesCard/SeriesCard";

export const SeriesList: React.FC = () => {
    return (
        <div>
            <h2>Список сериалов:</h2>
            <SeriesCard/>
        </div>
    );
}